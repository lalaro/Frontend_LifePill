<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="com.escuelaing.edu.lifepill">

    <!-- ========== PERMISOS ========== -->
    <!-- Internet: Requerido para llamadas HTTP/HTTPS -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Estado de red: Para verificar conexión -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Notificaciones: Para Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.LifePill"
            android:usesCleartextTraffic="true"
            android:networkSecurityConfig="@xml/network_security_config"
            tools:targetApi="31">

        <!-- ⚠️ IMPORTANTE:
             android:usesCleartextTraffic="true" - Permite HTTP (desarrollo)
             android:networkSecurityConfig - Configuración de seguridad de red
             En PRODUCCIÓN: cambiar usesCleartextTraffic="false" y usar HTTPS
        -->

        <activity
                android:name=".MainActivity"
                android:exported="true"
                android:label="@string/app_name"
                android:theme="@style/Theme.LifePill"
                android:windowSoftInputMode="adjustResize">
            <!-- windowSoftInputMode: Para que el teclado no cubra los campos -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>